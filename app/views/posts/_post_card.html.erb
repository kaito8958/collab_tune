<div id="post-card-<%= post.id %>" 
     class="w-full max-w-3xl bg-white rounded-2xl shadow-md border border-gray-200 hover:shadow-lg hover:border-purple-300 transition-all duration-300 mx-auto mb-6 p-5">

  <!-- 💡 募集状態バッジ -->
  <% if post.status.present? %>
    <div class="text-center mb-3">
      <% if post.recruiting? %>
        <span class="inline-block px-5 py-1.5 text-sm font-bold text-white rounded-full shadow-sm bg-green-500">
          🟢 コラボ募集中
        </span>
      <% else %>
        <span class="inline-block px-5 py-1.5 text-sm font-bold text-white rounded-full shadow-sm bg-gray-500">
          🔴 募集停止中
        </span>
      <% end %>
    </div>
  <% end %>

  <!-- 上段：タイトル / ユーザー / ジャンル -->
  <div class="flex justify-between items-start mb-3">
    <div>
      <h3 class="text-2xl font-bold text-gray-800 leading-tight">
        <%= link_to post.title, post_path(post), class: "hover:text-purple-600 transition-colors duration-200" %>
      </h3>
      <p class="text-xs text-gray-500 mt-1 flex items-center space-x-1">
        <span>
          by <%= link_to post.user.nickname, user_path(post.user), class: "hover:underline hover:text-purple-500" %>
        </span>
        <span>·</span>
        <span><%= time_ago_in_words(post.created_at) %>前</span>
      </p>
    </div>

    <!-- ジャンルバッジ -->
    <div class="text-xs font-semibold text-white px-3 py-1 rounded-full shadow bg-gradient-to-r from-orange-400 to-pink-400 whitespace-nowrap">
      <% if post.genres.present? %>
        <%= post.genres.map(&:name).join(" / ") %>
      <% else %>
        ジャンル未設定
      <% end %>
    </div>
  </div>

  <!-- 🎧 再生バー -->
  <div class="flex items-center space-x-4 bg-gray-100 rounded-xl p-3 mb-3 <%= 'opacity-60 cursor-not-allowed' unless post.audio.attached? %>">
    <button id="btn-<%= post.id %>" 
      onclick="togglePlay(<%= post.id %>)"
      class="bg-purple-600 hover:bg-purple-700 text-white rounded-full p-3 shadow-md flex-shrink-0 transition transform hover:scale-105"
      aria-label="再生"
      <%= 'disabled' unless post.audio.attached? %>>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 fill-white" viewBox="0 0 24 24">
        <polygon points="5 3 19 12 5 21 5 3"/>
      </svg>
    </button>

    <div class="flex-grow h-2 bg-gray-300/60 rounded-full relative overflow-hidden">
      <div id="progress-<%= post.id %>" class="absolute top-0 left-0 h-2 rounded-full w-0 transition-all duration-100" style="background: linear-gradient(90deg, #a855f7, #6366f1);"></div>
    </div>

    <p id="time-<%= post.id %>" class="text-[11px] text-gray-500 ml-2 whitespace-nowrap">0:00 / 0:00</p>
  </div>

  <% if post.audio.attached? %>
    <audio id="audio-<%= post.id %>" src="<%= url_for(post.audio) %>"></audio>
  <% end %>

  <!-- 💬 説明 + 募集パート -->
  <div class="text-gray-700 text-sm leading-relaxed mb-4">
    <p><%= truncate(post.description, length: 100) %></p>

    <% if post.looking_for_skills.present? %>
      <p class="text-xs font-semibold text-gray-600 mt-3 mb-1">🎸 募集中のパート：</p>
      <div class="flex flex-wrap gap-2">
        <% post.looking_for_skills.each do |skill| %>
          <span class="text-xs font-medium text-gray-700 bg-gray-100 border border-gray-200 rounded-full px-2.5 py-0.5">
            <%= skill.name %>
          </span>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- 下段：コメント数 / 詳細ボタン -->
  <div class="flex justify-between items-center text-xs text-gray-600">
    <div class="flex items-center space-x-2">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-gray-500" viewBox="0 0 24 24">
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
      </svg>
      <span><%= post.comments.count %> コメント</span>
    </div>

    <%= link_to post_path(post),
      class: "inline-block px-3 py-1.5 bg-gradient-to-r from-purple-600 to-indigo-600 text-white font-semibold rounded-full shadow-sm hover:from-purple-700 hover:to-indigo-700 hover:shadow-md transition-all text-xs" do %>
      詳細 →
    <% end %>
  </div>
</div>
