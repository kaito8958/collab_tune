<div class="message <%= message.user_id == current_user.id ? 'self' : 'other' %>">
  <div class="message-content">
    <%= simple_format(message.content) %>
    <time><%= message.created_at.strftime("%Y/%m/%d %H:%M") %></time>
  </div>
</div>